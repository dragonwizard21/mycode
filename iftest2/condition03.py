#!/usr/bin/env python3
""" Created by Zach Huff 9/15/2020, for the purpose of checking the users selection of hostname"""

hostname = input.lower("What name should the hostname be?: ") # This asks the user for input
print(hostname)
# Notice the change from the last two programs
#if hostname.lower() == "mtg":
#    print("The hostname was found to be MTG")

